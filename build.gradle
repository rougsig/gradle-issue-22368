plugins {
    id 'sample'
}

sample {
    someExampleValue = "Some Example" // This works

    innerWithClosure {
        someInnerValue = "Some Inner Value" // This works

        exampleFunctionWithClosure {
            someValue = "Example 1" // This works
        }
    }
}

sample {
    someExampleValue = "Some Example" // This works

    inner {
        someInnerValue = "Some Inner Value" // This works

        exampleFunction {
            someValue = "Example 1" // This doesn't work
        }
    }
}